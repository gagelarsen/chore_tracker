// Placeholder for future unit tests.
//
// The only types currently in the app target are SwiftUI `App`/`View` values,
// which CLAUDE.md §3 exempts from unit tests. `ContentView`'s happy path is
// covered by `ChorezUITests`. Real `@Test` cases land here as non-view types
// (state stores, view models, pure helpers) are added.
